# TOSA Tier A — NITO export, ANSYS post-processing, plotting
# ANSYS itself is installed separately (not pip).
# Add ansys-mapdl-reader when standardizing on .rst exports.

numpy>=1.24,<3
scipy>=1.10
matplotlib>=3.7
pandas>=2.0
pyyaml>=6.0
gdown>=5.0

# Optional visualization (authors_reading / nates_reading)
scikit-image>=0.21
pyvista>=0.42
